# DNS Resolution Script

## Purpose
A minimal Python script to resolve a domain name to its IPv4 address using Python’s built-in socket library.

## Use Case
Useful during OSINT investigations for quick infrastructure mapping and domain analysis.

## Requirements
Python 3.x

## Usage
Run the script and enter a domain name when prompted.
